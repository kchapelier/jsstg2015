"use strict";

//var objectCollection = require('../objectCollection');

module.exports = {
    postUpdate: function (element, dt) {
        //FIXME the explosion need to be removed from the collection and added back to the pool when it is finished
        //objectCollection.remove('enemyShot', element);
    }
};
